const INITIAL_STATE = {
    showModal: false,
    data: {},
    betAmount: "",
    toWin: "",
    selectedBet: {
        id: null,
        side: null,
        rubric: null,
        odds: null
    }
}

export default (state = INITIAL_STATE, action) => {
    switch(action.type) {

    }
}